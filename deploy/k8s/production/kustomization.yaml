apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vane
resources:
  - namespace.yaml
  - configmap.yaml
  - middleware-redirect.yaml
  - ingress-http.yaml
  - ingress-https.yaml
  - ../base
images:
  - name: ghcr.io/phuongddx/vane
    newTag: immutable
